# MVP pivot plan — status & next steps

## V0 re-scope as of 2026-07-06 — START HERE

**Decision (2026-07-06):** Pause all front-end and feature work. Step back and rethink the front end and the feature set from scratch. This supersedes the "Not yet built — pick up here" list in the older status section below — do **not** resume those items (radius selector, multi-framework preview slices, dirty-state UI, etc.) until the re-scope is settled.

### Step 3 (BUILT 2026-07-06): Theme Studio core loop + UI

The three-pane workspace from the sketch is built and verified in the browser at **`/studio`** (added to `PUBLIC_PATHS` in `src/lib/supabase/middleware.ts` — client-side, no auth/DB yet).
- **New files:** `src/app/studio/page.tsx`, `src/components/studio/theme-studio.tsx` (workspace: top bar, left rail nav, middle editor, export dialog), `src/components/studio/theme-preview.tsx` (live sample dashboard), `src/lib/studio/theme.ts` (derivation: seeds → ramps → semantic roles → `theme.css`).
- **Reuses:** `materialColorScale()` from `src/lib/gap-fill/material.ts` (deterministic ramp, 500=seed), `aestheticForeground()` from `oklch.ts`, and the CSS-var-override preview mechanism proven in `shadcn-framework-preview.tsx`.
- **Editor:** 6 color seeds (brand primary, brand secondary, neutral, success, warning, danger) each with a live 50–950 ramp; Typography (font family); **Spacing (global density presets — Compact→Expansive, 7 steps — that set Tailwind's base `--spacing` unit, which every padding/gap/size utility multiplies off, so one value rescales the whole UI's density; verified the build emits `calc(var(--spacing) * n)` so the wrapper override cascades)**; Radius (segmented Sharp→Pill); Semantic Roles read-only list showing each shadcn role's resolved value per mode.
- **Typography — Google Fonts / Custom (2026-07-06):** Typography uses its own **Google Fonts | Custom** toggle (not the generic Generated/Manual). **Google Fonts** = searchable picker over the full ~1,936-font catalog (`src/lib/google-fonts.ts`, names only, paginated 50/scroll — no per-option font loading); selecting lazily injects a cached `<link>` (`display=swap`, weights 400–700). **Custom** = paste a URL (a `.css` stylesheet → `@import`; a `.woff2/.ttf` file → `@font-face`) OR upload a font file (loaded via a local object URL for preview; export emits a self-host note since blob URLs can't ship). All loaders live in `src/lib/studio/google-font-loader.ts` (`loadGoogleFont`/`loadStylesheet`/`loadFontFace`/`loadCustomFont`, cached by hashed id); a top-level effect keyed on `fontSource`/`fontName`/`customFont` (re)loads on undo. `ThemeConfig` now carries `fontSource` + `fontName` + `customFont`; `buildFontPrelude()` in theme.ts produces the right `@import`/`@font-face`/note per case. Perf verdict (prior turn): listing names is free; only the one chosen font file loads. Curated `FONT_OPTIONS` presets retired from the UI.
- **Font applies to WHOLE preview + upload self-host export (2026-07-06):** Preview font-swap was leaking: shadcn `.font-heading` compiles to `font-family: var(--font-sans)` and `.font-sans` to `var(--font-dm-sans)` (verified in the served CSS), so setting only `fontFamily` on the wrapper left card titles/stat numbers on the app default. Fix: `theme-preview.tsx` wrapper now also overrides `--font-dm-sans`/`--font-sans`/`--font-heading` (mono left untouched). Upload export upgraded: `buildFontPrelude()` now emits a real ready-to-paste `@font-face` at `/fonts/<filename>` with `format()` + a "save file here" note (we deliberately do NOT host uploaded fonts — licensing/redistribution risk; MCP stays CSS-only, user/agent self-hosts the binary). See the hosting-vs-self-host analysis discussion.
- **Generated vs Manual + Undo (2026-07-06):** Color, Spacing, Radius, and Typography each have a **Generated | Manual** toggle (Radius/Typography manual added 2026-07-06; Radius-manual = base slider + read-only derived sm/md/lg/xl). Color-manual exposes every one of the 66 ramp steps (6 families × 11) as its own editable color swatch; Spacing-manual is a base-unit slider (0.15–0.6rem) with the read-only derived scale. Color source of truth moved to a working `ramps` field on `ThemeConfig` (seed-generated OR hand-edited) that `deriveTheme`/export/preview all read — so manual edits cascade through the same alias path as seeds. **Undo** (top bar + left rail) via a snapshot-history reducer (`useThemeHistory`) with per-field coalescing so a color-drag is one undo step; **Reset all** restores defaults (itself undoable). Switching Manual→Generated regenerates from seeds and discards manual edits (recoverable via Undo).
- **Verified live (real browser):** editing a seed cascades instantly through aliased roles (changed primary seed → `--primary`/`--ring`/`--chart-1`/`--sidebar-primary` all updated); light/dark toggle swaps the var set; export dialog emits valid `:root`+`.dark`+`@theme inline` shadcn-compatible `theme.css`; no console errors.
- **Known gaps / next:** no persistence (state is in-memory React only — no save/load, no design-system selector wired to DB); "Settings" nav is a disabled placeholder; Import side of "Import/Export" not built (export only); MCP delivery of the generated theme not wired; light-mode preview renders while under the app's global `.dark` html class, so shadcn `dark:` utility branches technically still apply (minor — main colors come from overridden vars so it reads correctly); deterministic ramp quality still the load-bearing risk ([[gap-fill-design-quality]]).

**What we keep (unchanged, not touched by this pivot):**
- **Backend** — MCP server (`supabase/functions/design-system-mcp/`), presenters (`lib/present.ts`).
- **Database** — Supabase project `gxpqjpwsswhgdufyyxlt`, schema/migrations (`supabase/migrations/`), and the `master-reference` fixture (id `f994089e-fff3-4870-839f-233af1757628`).
- **Notes** — all planning docs (`PIVOT-PLAN.md`, `MVP-SCOPE.md`, `MARKET-RESEARCH.md`, `MARKETING-ASSESSMENT.md`) and the `Design files backend/` reference folder.
- **Parsers** (`src/lib/parsers/`), **accessibility checker** (`src/lib/a11y/`), **token type system** (`src/types/tokens.ts`).

**V0 scope constraint — Tailwind only:** For V0, **Tailwind CSS is the only front-end design system we offer.** Drop shadcn / Bootstrap / css-variables / Figma / Mantine as *offered* export targets for V0 (the presenters can stay in the codebase, just not surfaced in the product). The token model, the UI, and the export/MCP output should all be organized around Tailwind's theme-variable model. Rationale: one framework done excellently beats five done partially; Tailwind's `@theme` system is itself a clean, opinionated design-token vocabulary we can adopt as our canonical model rather than inventing our own.

### Step 1 (done 2026-07-06): Tailwind theme system — reference

Read from the live docs (https://tailwindcss.com/docs/theme + /docs/colors). Distilled for our purposes:

**Core mechanic.** Tailwind v4 is CSS-first. You `@import "tailwindcss";` then declare design tokens in an `@theme { … }` block. A theme variable is not just a CSS var — declaring `--color-mint-500: oklch(…)` *generates the utility classes* (`bg-mint-500`, `text-mint-500`, `fill-mint-500`, …) **and** exposes `var(--color-mint-500)` for custom CSS/JS. This is the key insight for us: **a Tailwind theme *is* a token set.** `@theme` = tokens-that-make-utilities; `:root` = plain vars with no utilities.

**Namespaces (this is our V0 token vocabulary).** Each namespace maps to a family of utilities/variants:

| Namespace | Drives | Our token category |
|---|---|---|
| `--color-*` | `bg-`, `text-`, `border-`, `fill-`, `ring-`, … (+ `/opacity` modifier) | color |
| `--font-*` | `font-sans` etc. | font-family |
| `--text-*` (+ paired `--text-*--line-height`) | `text-xl` etc. | font-size (with bundled line-height) |
| `--font-weight-*` | `font-bold` | font-weight |
| `--tracking-*` | `tracking-wide` | letter-spacing |
| `--leading-*` | `leading-tight` | line-height |
| `--breakpoint-*` | `sm:` `md:` responsive variants | breakpoint |
| `--container-*` | `@sm:` container queries + `max-w-md` | sizing/container |
| `--spacing-*` (base `--spacing`) | `px-4`, `max-h-16` (multiplied off one base unit) | spacing |
| `--radius-*` | `rounded-sm` | border-radius |
| `--shadow-*` / `--inset-shadow-*` / `--drop-shadow-*` / `--text-shadow-*` | `shadow-md`, `drop-shadow-*`, `text-shadow-*` | shadow family |
| `--blur-*` | `blur-md` | blur/effects |
| `--perspective-*`, `--aspect-*`, `--zoom-*` | perspective/aspect/zoom | effects |
| `--ease-*` | `ease-out` | easing |
| `--animate-*` (+ `@keyframes`) | `animate-spin` | animation |

**Override / extend / disable.**
- *Extend*: add a new var inside `@theme` (defaults stay).
- *Override*: redeclare a default var (`--breakpoint-sm: 30rem;`).
- *Clear a namespace*: `--color-*: initial;` then declare only what you want.
- *Clear everything*: `--*: initial;` for a fully bespoke theme.
- *Disable one value*: `--color-lime-*: initial;`.

**Referencing other vars — `@theme inline`.** If a token points at another variable (e.g. a runtime `:root`/`[data-theme="dark"]` var), you must use `@theme inline { --color-canvas: var(--acme-canvas-color); }` so the utility embeds `var(...)` rather than resolving at definition time. This is exactly the pattern for **light/dark mode**: define the raw values in `:root` / `[data-theme="dark"]`, alias them into `@theme inline`. Relevant to our primitive→semantic aliasing constraint.
- `@theme static` — force-emit *all* vars (not just used ones), useful when we generate a themeable stylesheet consumers will read at runtime.

**Colors.** 22 default palettes (red, orange, amber, yellow, lime, green, emerald, teal, cyan, sky, blue, indigo, violet, purple, fuchsia, pink, rose, slate, gray, zinc, neutral, stone) × 11 shades (50–950), plus `black`/`white`, all authored in **OKLCH**. Opacity via `/` modifier (`bg-sky-500/50`, arbitrary `/[71.37%]`, or `/(--var)`). In custom CSS, adjust alpha with `--alpha(var(--color-gray-950) / 10%)`.

**Selected default values worth pinning** (so our defaults match Tailwind's expectations):
- `--spacing: 0.25rem` (all spacing/sizing multiply off this one unit).
- Breakpoints: sm 40rem / md 48rem / lg 64rem / xl 80rem / 2xl 96rem.
- Radius: xs .125 / sm .25 / md .375 / lg .5 / xl .75 / 2xl 1 / 3xl 1.5 / 4xl 2 rem.
- Font weights: thin 100 → black 900 in 100s (normal 400, medium 500, semibold 600, bold 700).
- `--text-*` ship a paired `--text-*--line-height` (e.g. `--text-2xl: 1.5rem; --text-2xl--line-height: calc(2 / 1.5);`) — our font-size tokens should carry a line-height alongside to match.
- Default eases: in `cubic-bezier(0.4,0,1,1)`, out `(0,0,0.2,1)`, in-out `(0.4,0,0.2,1)`.
- Built-in animations: spin/ping/pulse/bounce with their `@keyframes`.

**Distribution.** Themes are shareable as a plain CSS file (`@import "../brand/theme.css";`) or an npm package. **This is our natural V0 export/MCP artifact:** emit a `@theme { … }` block that a consumer drops into their `app.css`.

### Step 2: front-end & feature spec (defined 2026-07-06 from Ben's sketch)

**Product goal / positioning:** *"Design your theme once. Use it everywhere."* Author a theme in one place; ship it to multiple destinations:
- **Vibe-coding tools** (Cursor / v0 / Claude etc.) — copy-paste-ready `@theme` block / rules.
- **Tailwind theme exports** — a `theme.css` (`:root` + `.dark` + `@theme inline`).
- **Figma library** — deferred to a later phase (V2), not V0.
(The MCP server we already have is the natural delivery channel for the "use it everywhere" promise.)

**Token model = two tiers (locked):**
- **Primitives** — the Tailwind `@theme` namespaces (color scales, font-family/size/weight, spacing, radius, shadows, …). Authored directly.
- **Semantic tokens** — the shadcn roles (`primary`, `secondary`, `background`/`foreground`, `card`, `muted`, `border`, `ring`, `chart-*`, `sidebar-*`, …). These **alias to primitives**, never store raw values ([[primitive-alias-constraint]]) so edits cascade.
- **Brand colors are primitives:** primary + secondary brand are authored as primitive palettes (50–950); the `primary`/`secondary` semantic roles alias to them. (Dropped "accent" as a designer term — see brand/accent discussion. shadcn's own `accent` role stays a near-neutral hover surface, auto-derived.)

**UI — three-pane workspace (from sketch):**
1. **Left rail — token navigation.** Two collapsible groups: **Primitives** (Color, Font Family, Font Size, Font Weight, …) and **Semantic Tokens** (the shadcn roles). Bottom utilities: **Import/Export**, **Reset**, **Dark Mode** toggle, **Settings**.
2. **Middle pane — variable editor** for the selected category. E.g. selecting a color palette (like "Brand color") shows its editable scale (`-50` … `-950`) as swatch rows.
3. **Right pane — live sample product UI / fake dashboard** that re-themes **instantly** as variables change — CSS-variable override on a real shadcn/Tailwind preview, no rebuild. Evolves the existing `src/components/shadcn-framework-preview.tsx` mechanism.

**Top bar:** logo · "Select Design System ▾" · "New Design System" · account.

**Why this is coherent / already-proven:** instant re-theming = swapping CSS vars on the live preview (already working for the shadcn slice). It *requires* the alias model — editing `brand-500` must ripple through `--primary` into every previewed component — which is exactly the tier structure above.

**Resolved 2026-07-06 — brand color is seed-based:** the user picks **one** brand color and we generate the full `50–950` ramp from it (not hand-authoring 11 shades). One seed per brand palette (primary brand → ramp, secondary brand → ramp).
- **Follow-on decision (resolved 2026-07-06) — deterministic only:** V0 color ramps are generated **purely deterministically** via OKLCH math (`src/lib/gap-fill/material.ts`) — instant and free on every seed change. **AI color-fill (`src/lib/ai/color-fill.ts`) is OUT of V0 scope** (not even an opt-in polish action); it stays in the codebase, deferred to a later phase. The a11y checker (`src/lib/a11y/`) still validates/flags generated values. Known risk to watch: deterministic ramps can look flat vs Tailwind's hand-tuned palettes — a pain point in earlier sessions ([[gap-fill-design-quality]]); ramp quality must be revisited before calling V0 done, since there's no AI fallback now.
- **Resolved 2026-07-06 — semantics seed from user-picked colors, not auto-derived:** for V0 the user picks **seed colors** for the color scales; we generate the ramps; the shadcn semantic roles alias into those generated primitives. No auto-deriving neutrals/roles from a single brand seed ("for now" — keep it simple). V0 color seed inputs ≈ brand primary, brand secondary, a neutral/gray (drives `background`/`foreground`/`muted`/`border`/`card`/`sidebar`), and optionally status hues (success/warning/danger). Each seed → deterministic OKLCH ramp; roles map e.g. `--primary: var(--color-brand-primary-600)`, `--muted: var(--color-neutral-100)`.

**V0 essential vs. deferred:** essential = color primitives (incl. brand primary/secondary), radius, core typography (family/size/weight), and the shadcn semantic color roles + light/dark; the live preview; export as `@theme`/`theme.css`; MCP delivery. Deferred = Figma export, the long tail of effect namespaces (perspective/zoom/aspect/text-shadow), and UX tokens (date formatting, tone of voice).

---

## Status as of 2026-06-25 — (historical; superseded by the V0 re-scope above)

**Fully built and verified** (real browser clicks, real API calls, real DB writes — not just code review):
- All 4 V1 target frameworks proven through real consumers: css-variables, Tailwind v4, shadcn, Bootstrap (see "Already built" / ingestion-test entries below).
- AI color-fill — replaces the deterministic color-scale step with a real Anthropic call (`src/lib/ai/color-fill.ts`), gated behind the new "Update design system" action, metered via `ai_usage` (25 calls/month). See "Open questions" → AI-fill entries for the full build/bug-fix history.
- Freeform/unstructured-text ingestion — `src/lib/ai/freeform-ingest.ts`, "Describe it" tab, same 25/month metering pattern.
- Visual preview, Pass 1, shadcn slice only — real shadcn/ui components re-themed live via CSS variable overrides, no build step. Tailwind v4/css-variables slices of Pass 1 NOT built yet; Pass 2 (Bootstrap/Tailwind v3, needs real Sass compile) not designed.
- AI color-fill system prompt tuned for modern/tinted-neutral aesthetics (2026-06-25), verified with real calls.

**Tried and explicitly reverted** (don't redo without new information): pushing the deterministic `success`/`warning`/`danger` semantic-color defaults more vivid — real contrast-ratio testing showed it regressed two of three from passing to failing AA as plain text on `color.background`. Original values are back in place.

**Not yet built — pick up here:**
1. **Quick Start radius selector UX** — `src/components/quick-start-form.tsx` has a `<select>` dropdown for border radius. Replace it with a segmented button group (5 options: Sharp → Pill) so designers can see and pick the feel at a glance without opening a dropdown. Keep the existing `name="radius"` / `defaultValue={currentRadius ?? "md"}` logic unchanged — purely a visual/UX improvement. Prompt: *"In `src/components/quick-start-form.tsx`, replace the border radius `<select>` dropdown (the `RADIUS_OPTIONS` block) with a segmented button group. Render all 5 options as pill-shaped toggle buttons in a row — selected state gets filled/active style, others are outlined. Use a hidden `<input type='hidden' name='radius'>` to track the selected value so form submission is unchanged. Keep `defaultValue={currentRadius ?? 'md'}` logic. Match the shadcn visual style of the rest of the form."*
2. Visual preview: Tailwind v4 + css-variables slices of Pass 1 (shadcn already proved the mechanism — same CSS-variable-override approach should work for plain Tailwind utility classes).
3. Visual preview Pass 2: Bootstrap/Tailwind v3 (needs a real Sass-compile-on-demand server endpoint, caching/debounce strategy undesigned).
4. Dirty-state UI signal — user has no visual cue that an edited color hasn't been committed via "Update design system" yet.
5. "Run gap-fill" conditional visibility — should hide once nothing is missing; currently always shows.
6. Product UI/chrome polish — direction only ("make it sexier"), needs a real scoping pass before it's buildable.
7. Figma export and the "10 UX principles" — both explicitly deferred to V2, not V1 work.

**Date started:** 2026-06-22
**Why:** Spent a long session iterating on algorithmic color generation (OKLCH/HSL math in `src/lib/gap-fill/material.ts`) and a rich live-preview UI. User still wasn't happy with generated palette quality after several rounds of fixes. Root cause: "does this look good" is a subjective design judgment math alone can't resolve, and we never validated whether the underlying product (tokens served via MCP) actually works with real tools. Decision: pause generation-quality polish, de-scope the UI, and validate the core product loop first.

**What stays as-is:** Database schema (`supabase/migrations/`), parsers (`src/lib/parsers/*`), MCP server (`supabase/functions/design-system-mcp/`), accessibility checker (`src/lib/a11y/`). These are considered solid — see "Already built" below.

**What's being shelved for now:** The algorithmic gap-fill color math (`src/lib/gap-fill/`) and the rich `DesignSystemPreview` UI. Not deleted — just not the focus. Revisit only after the ingestion/AI-fill loop is validated.

## The plan (5 steps)

1. **Master semantic token list → MCP → test real-tool ingestion.** Write a canonical file of all tokens with descriptive semantic names (not just primitives). Confirm the MCP serves it in a shape Figma, Tailwind, Bootstrap, and shadcn can actually ingest.
2. **Test AI ingestion of pasted variables.** Give an AI a cut-and-paste blob of a user's existing variables; see what it can map automatically and what it calls out as missing/ambiguous.
3. **AI populates missing tokens** — replaces (or augments) the deterministic gap-fill math from this session.
4. **Build UI for customizing UI elements** (after 1-3 are validated, not before).
5. **Build UI to toggle UX tokens** (date formatting, tone of voice, etc.) — start with 2-3 toggles, not the full "10 principles" list, to prove the concept cheaply. Confirm these are actually delivered correctly through the MCP.

**Decided constraint for step 3:** Don't let AI fully replace the deterministic checks. AI proposes values; the existing contrast checker (`src/lib/a11y/`) and contrast-anchored dark-CTA logic (`src/lib/gap-fill/material.ts` → `ctaDarkPair`) validate/flag anything that fails AA. Hybrid, not pure-AI — keeps this session's accessibility work as a safety net instead of discarding it.

**Monetization for now:** No paywall while testing. AI generator usage may eventually be metered (free uses included, pay for more) — not needed until the product loop is validated.

## Already built (don't rebuild)

- **✅ Exhaustive master reference design system — built this session.** Investigated the actual DB: every existing design system (including "Testing 123") only ever covered 6 of the 26 `TokenCategory` values defined in `src/types/tokens.ts`, because gap-fill itself only knows how to generate those 6. There was no row-level catalog covering the full schema vocabulary. **Built one**: design system `master-reference` (id `f994089e-fff3-4870-839f-233af1757628`, slug `master-reference`, `is_public: true`) in the live Supabase project (`gxpqjpwsswhgdufyyxlt`). It has **185 tokens / 291 values covering all 26 categories** — the original 136 (color scales, border-radius, breakpoint, font-family, font-size, spacing) copied from "Testing 123", plus 49 new tokens for the previously-missing 20 categories: `font-weight`, `line-height`, `letter-spacing`, `paragraph-spacing`, `text-decoration`, `text-transform`, `shadow`, `drop-shadow`, `text-shadow`, `border-width`, `border-style`, `border`, `opacity`, `z-index`, `duration`, `easing`, `transition`, `animation`, `sizing`, `component`. Has both `light` and `dark` modes (color tokens vary by mode; everything else is mode-agnostic, living only under `light`, matching the existing convention in the DB). **This is now the canonical fixture for ingestion testing.** **Still not present:** any UX tokens (date formatting, tone of voice) — confirms step 5 is genuinely greenfield, nothing to build on there yet.
- **Parsers** (`src/lib/parsers/`): dtcg, tokens-studio, tailwind-v3, tailwind-v4, shadcn, bootstrap, mantine, figma-variables, js-object-literal. Import side is in good shape across most target formats.
- **MCP server** (`supabase/functions/design-system-mcp/index.ts` + `lib/present.ts`): working Deno edge function, JSON-RPC 2.0 over HTTP, serves tokens keyed by design-system slug, has public/private access control via `mcp_token`. Tools include token listing and `get_accessibility_report`.
- **✅ Export/present shaping for all 4 target tools — built and deployed this session.** `lib/present.ts` → `presentTokens()` now supports `css-variables`, `tailwind-v4`, `shadcn`, `dtcg`, `tailwind-v3`, **and the two that were missing: `bootstrap` and `figma-variables`**. Also extended `toCssValue` with cases for `shadow`, `border`, `transition` (previously only `color`/`dimension`/`duration`/`fontFamily`/`fontWeight`/`number`/`cubicBezier`/`string`/`boolean` were handled — these three were silently dropping every token in the 20 newly-added categories). `presentFigmaVariables` produces the *exact* `{meta:{variables, variableCollections}}` shape that `src/lib/parsers/figma-variables.ts` reads back in — true round-trip, not just Figma-flavored JSON. `presentBootstrap` outputs SCSS variable names for known roles (`$primary`, `$body-bg`, etc.) with a generic `$path-name` fallback for everything else, plus a `$grid-breakpoints` map via `document`. Deployed as `design-system-mcp` v3 (Supabase project `gxpqjpwsswhgdufyyxlt`). **Live-tested against `master-reference`:** `bootstrap` and `figma-variables` both hit **0/185 unmapped**. Regression-checked the other 5 frameworks — no breakage; their non-zero unmapped counts (`tailwind-v4`: 41, `shadcn`: 173, `tailwind-v3`: 39) are pre-existing/by-design (those presenters were already selective about which categories they name) and were the same gap, not something I introduced. **New known gap surfaced by this test:** `tailwind-v4` and `tailwind-v3` have no explicit namespace/category handling for the 20 newly-added categories (shadow, border, opacity, z-index, sizing, component, etc.) — `namespaceForV4` and `presentTailwindV3`'s category branches were never extended because no design system had those categories to test against until `master-reference` existed. Not fixed yet — flagged as a follow-up, not done.
- **Accessibility checker** (`src/lib/a11y/`): WCAG contrast checks per mode, including the alert/badge-as-text-on-tinted-background pattern (fixed this session — was previously a blind spot). Reports only, does not auto-fix, per explicit user instruction.
- **Token type system** (`src/types/tokens.ts`): canonical `ParsedToken` shape, `TokenCategory`/`TokenType` enums, `SupportedFormat` union already lists all 4 target ingestion formats (figma-variables, tailwind, bootstrap, shadcn) plus more.

## Concrete next action (partially done — pick up here)

Done: master reference dataset, both missing presenters (bootstrap, figma-variables), API-level testing (0 unmapped for both new frameworks, no regressions on the other 5).

**✅ Also done:** Added 21 missing shadcn-required color tokens (`card`, `popover`, `accent`, `input`, `ring`, `chart.1-5`, `sidebar.*` — 8 sidebar sub-tokens) to `master-reference`, derived sensibly from existing palette (card/popover ← background, accent ← muted, chart.1-5 ← primary/secondary/success/warning/danger, sidebar.* ← muted/primary/border equivalents). Extended `namespaceForV4` and `presentTailwindV3` with mappings for all 20 previously-uncovered categories. Redeployed as v4 and re-tested all 7 frameworks:
- `tailwind-v4`: 41 → **0 unmapped**
- `tailwind-v3`: 39 → **0 unmapped**
- `shadcn`: variable count 12 → **33** (now populates every slot shadcn's own theme defines — card/popover/accent/input/ring/chart/sidebar were previously silently defaulting). Unmapped count unchanged (173) — this is correct and expected, not a bug: shadcn's theme genuinely has no concept of color scale steps, shadows, typography details, etc., so those will always be out of scope for that format specifically.
- All other frameworks unchanged at 0 unmapped.

**✅ Also done:** Added a plain GET REST route alongside the JSON-RPC MCP protocol — `/design-system-mcp/<slug>?framework=X&mode=Y` returns the same presented tokens as raw JSON (no JSON-RPC envelope), for tools/agents that don't speak MCP but can `fetch()` a URL. Deployed as v5. Refactored both the GET handler and the POST `get_tokens`/`get_theme` tools to share one `getPresentedTheme()` helper — no duplicated logic. Live-tested: correct 401 on private design systems without a token, 404 on bad slugs, full-theme (all modes) when `mode` is omitted. Added a framework/mode picker to `McpAccessCard` (`src/components/mcp-access-card.tsx`) so users can generate and copy this URL from the dashboard — **not visually verified in-browser** (no login credentials available this session), only confirmed via `tsc --noEmit` and code review.

**✅ Real ingestion test — done, and it worked.** Fetched the live `master-reference` GET endpoint fresh (not from memory), extracted the actual `css-variables` output (206 light + 127 dark vars), and built a real static HTML prototype (a small "Account settings" page: cards, form fields, buttons, badges, alert, disabled state) using **only** `var(--...)` references to those tokens — zero hardcoded colors/fonts/spacing. Rendered via a local static server and screenshotted in both modes:
- Light mode: green primary (`#2a6500`-derived), magenta secondary (`#dd4673`-derived), correct success/warning/accent badge colors, visible `opacity-disabled` on the disabled button, decorative cursive `Fuggles` font exactly as the data specifies.
- Dark mode toggle: background correctly flips near-black, all colors relighten appropriately, borders adjust — **and the font changes to `"Abhaya Libre", serif`**, faithfully reflecting a real inconsistency in the underlying data (light/dark modes have different font-family values, which is arguably a data bug) rather than masking it.
- This is the proof the pivot was chasing: an external consumer using only the MCP/GET output, with no extra knowledge, produces a UI that visibly and correctly reflects the design system — confirms the core product loop works end-to-end for at least the `css-variables` framework.
- **Not yet tested this way:** Bootstrap (would need actual SCSS compilation), Figma (would need the actual Figma plugin), shadcn/Tailwind (would need a real Next.js+Tailwind build, not just static CSS vars). The `css-variables` proof is the strongest/easiest case since it needs no build step — the others remain to be proven the same way.

**✅ Fixed: mode-agnostic token drift.** The font-family inconsistency above was root-caused, not just noticed — traced via `created_at`/`updated_at`/`raw_value` columns to two independent per-mode imports (light edited later via the font combobox, dark never touched again), confirmed as the same bug pattern in another design system too ("another test": Assistant vs Aclonica). Fixed at the serving layer: `getPresentedTheme` (`supabase/functions/design-system-mcp/index.ts`) now resolves every category except `color` to ONE canonical value (preferring the default mode's row) and serves that same value for every mode, regardless of what's actually stored per-mode in the DB — protects against this drift for any design system, not just the fixture. Also deleted the orphan dark-mode `font-family.base` row in `master-reference` so the underlying fixture data is clean too. Deployed as v6. Verified: light/dark now both report identical `--font-family-base` and `--border-radius-base`, while `--color-background` still correctly differs per mode.

**✅ Real ingestion test — Tailwind v4 + shadcn, done, and it worked.** Scaffolded an actual Next.js + Tailwind v4 project via `create-next-app`, ran the real `shadcn@latest init`/`add` CLI (button, card, badge, input, label, select, alert — genuine shadcn-generated component files, not hand-rolled), fetched live `framework=shadcn` tokens from `master-reference`, and spliced them into the real shadcn-generated `globals.css` `:root`/`.dark` blocks (kept all of shadcn's own `@theme inline` plumbing untouched). Built a settings-page prototype using the actual imported shadcn components. Ran via real `next dev` (Turbopack) — required setting `turbopack.root` in `next.config.ts` since Turbopack was inferring the parent ToknHost repo as workspace root from a sibling lockfile and pulling in its `proxy.ts`/Supabase middleware by mistake (a real, generalizable gotcha for anyone testing a scratch Next.js project nested inside another one). Screenshotted both modes:
- Light: green primary, magenta secondary, correct badge variants, decorative `Fuggles` font rendering as the page heading font.
- Dark: background flips to black, colors relighten via shadcn's own `.dark` overrides, `Disabled` button correctly dims via shadcn's `disabled:opacity-50`.
- One real (and correct) shadcn behavior discovered, not a bug: shadcn's current `Button`/`Badge` "destructive" variant intentionally renders as a *soft tint* (`bg-destructive/10 text-destructive` in light, `/20` in dark) rather than a solid fill — our token value is fine, that's shadcn's own current design choice.
- This proves Tailwind v4 and shadcn ingestion for real, through their actual compilers — not just API-shape verification. Combined with the earlier `css-variables` static-HTML proof, the two easiest/most-common framework targets are now both genuinely proven end-to-end.

**✅ Real ingestion test — pure Tailwind v4 (no shadcn), done, and numerically verified.** Per user priority ("focus on Tailwind first, most common with vibe tools"), tested the raw `tailwind-v4` namespace standalone — the earlier test only exercised it underneath shadcn's own narrower variable set, which left the broader 26-category `@theme` mapping (`--text-*`, `--radius-*`, `--shadow-*`, `--color-{role}-{step}`, etc.) unproven on its own. Scaffolded a second scratch Next.js + Tailwind v4 project with **no shadcn at all**, fetched live `framework=tailwind-v4` output, pasted it directly into a plain `@theme { }` block, and wrote a page using only literal Tailwind utility classes (`bg-primary-500`, `text-xs`, `rounded-md`, `shadow-md`, etc.) — no dynamic class-name construction, since Tailwind's compiler only detects literal strings in source. Verified via `getComputedStyle` in the running browser, not just visual screenshot:
- `text-xs/sm/lg/xl/2xl/3xl` and `rounded-sm/md/lg` all computed to our exact values (10px/13px/20px/25px/31px/39px and 4px/8px/16px) — every one diverges from Tailwind's shipped defaults (12px/14px/18px/20px/24px/30px and 2px/6px/8px), so this can only be coming from our override, not a coincidental default match.
- `bg-primary-500` rendered a real solid color — meaningful because Tailwind ships zero default for any color named "primary," so this only renders at all if our `@theme` injection worked.
- This is the strongest proof yet since it's both numerically asserted (not eyeballed from a screenshot) and isolates the raw 26-category namespace from shadcn's narrower 33-slot layer — confirms vibe-coding tools that emit plain Tailwind utility markup (the more common case than full shadcn component usage) will render correctly.

**✅ Found and fixed a real bug while testing live: gap-fill generated ~154 spurious color tokens.** Running gap-fill a second time on `master-reference` (which already has the 21 shadcn-role colors) caused `computeGapFill`'s base-color scale generator to treat `card`/`popover`/`accent`/`input`/`ring`/`chart.*`/`sidebar.*` as "base colors needing a 50–950 scale," since they passed the existing `SCALE_SUFFIX_RE` check and had a resolvable hex. This broke ingestion completely (every framework went from 0 unmapped to 100% unmapped, since path counts no longer matched what presenters expected). Root-caused via `tokens`/`category` count breakdown, not guessed. Fixed with a `NON_SCALE_COLOR_ROOTS` denylist in `src/lib/gap-fill/index.ts`; verified with a new scenario-specific regression test reproducing the exact failure, plus the existing e2e script (no change to the happy path), plus a real `npm run build`. Cleaned the spurious tokens out of `master-reference` directly (master-reference also got toggled back to `is_public: true` after being flipped off mid-session, presumably via dashboard interaction — restored since the fixture needs to be public for unauthenticated GET testing).

**✅ Real ingestion test — Bootstrap, done, and it found two more real bugs before passing.** Scaffolded a Node project with real `bootstrap` + `sass` (Dart Sass) installed, fetched live `framework=bootstrap` output, wrote it as SCSS `!default` overrides preceding `@import "bootstrap/scss/bootstrap"`, and ran a real compile. Two genuine bugs surfaced and were fixed in `presentBootstrap`/`toBootstrapValue` (`supabase/functions/design-system-mcp/lib/present.ts`), deployed as v7/v8:
1. **Color values were OKLCH CSS strings, which Sass can't parse.** Bootstrap's own SCSS source calls Sass-native color functions internally (`shade-color()`/`tint-color()`, used for hover states and `-subtle`/`-emphasis` variants) which call Sass's built-in `mix()` — this threw `"$color2: oklch(...) is not a color"` immediately on `@import`. Unlike Tailwind v4/shadcn (browser parses the CSS function at paint time), Sass compiles ahead-of-time and needs a value it can parse itself. Fixed: Bootstrap-specific color resolution always uses hex.
2. **Dimension values were px, causing an "incompatible units" error deep in Bootstrap's internals** — e.g. `$nav-link-height: $font-size-base * $line-height-base + $nav-link-padding-y * 2` mixes our px-based `$font-size-base` with Bootstrap's own rem-based `$nav-link-padding-y` default, and Sass refuses to add `24px + 1rem`. Bootstrap's own `_variables.scss` defaults are rem throughout for exactly this reason. Fixed: all Bootstrap dimensions convert px→rem, **except breakpoints**, which Bootstrap's own `$grid-breakpoints` map keeps in px.
3. **(Caught by a Sass warning, not yet a hard error) `$grid-breakpoints` map must be sorted ascending and start at 0** — Bootstrap asserts this at compile time. Our breakpoints were emitted in arbitrary key-insertion order with no zero floor. Fixed: `presentBootstrap` now sorts ascending and synthesizes an `xs: 0` entry if missing.

After all three fixes, the compile succeeded with **zero errors and zero warnings**. Confirmed in the actual compiled CSS output (not just absence of errors): `--bs-btn-bg: #2a6500` (our exact primary hex), `--bs-btn-hover-bg: #245600` (Bootstrap's `shade-color()` correctly darkening it — the exact function that threw the original error), `--bs-body-font-size: 1rem` (16px→1rem), `--bs-border-width: 0.125rem` (2px→0.125rem), `--bs-border-radius: 0.5rem` (8px→0.5rem). Rendered a real page (buttons, badges, alert, card) using the compiled CSS and screenshotted it — all components render correctly with our brand colors and radius.

**Status: all 4 V1 target frameworks now proven through real consumers** (css-variables, tailwind-v4, shadcn, bootstrap). Figma was the 5th candidate but is deferred to V2 (see "V2 — explicitly deferred past launch" below) — never made it past API-shape verification, and needs the user's own Figma account to verify properly.

**✅ Found and fixed the most consequential bug yet — reported by a real external AI tool consuming the MCP, not found in-house.** User had a real vibe-coding tool (Lovable) build against `master-reference`'s `tailwind-v4` output. Lovable used `bg-primary-500` for a primary action button — a reasonable choice, since "500 is the base/main color" is the convention in Tailwind, Radix, Material, Ant Design, etc. — and got an unrelated bright lime green instead of the actual brand color (`#2a6500`, a dark forest green).

Root cause, confirmed by reading `materialColorScale` (`src/lib/gap-fill/material.ts`): the function only ever extracted **hue and saturation** from the seed color. Lightness for every step (including 500) came from a fixed, seed-independent ladder (`HSL_LIGHTNESS = [97,94,86,76,64,50,39,30,21,14,9]`) — step 500 was *always* exactly 50% HSL lightness, regardless of the seed's actual lightness. Since `#2a6500`'s real lightness is ~20%, it landed nowhere near step 500 — closer to step 800. **This isn't an edge case — it happens for almost every real brand color**, since most aren't naturally at 50% lightness. The literal brand hex a user picks essentially never appears anywhere predictable in its own generated scale.

Immediate guidance given for the live case: use `color.primary` (the flat, unsuffixed token — always the exact literal brand hex) rather than `color.primary.500` for primary actions; the numbered scale is a tint/shade ramp, not a guarantee of containing the seed.

Real fix implemented in `materialColorScale`: step 500 now always equals the seed hex exactly; the other 10 steps are proportionally interpolated FROM the seed's own lightness toward a light ceiling/dark floor (preserving the original ramp's relative spacing/shape), rather than an absolute fixed ladder. One implementation bug caught and fixed during verification: an earlier version clamped the *anchor* used for neighbor-step interpolation while keeping step 500 at the *unclamped* literal seed — for extreme seeds (near-white/near-black) this broke monotonic ordering (step 500 ended up lighter than step 400). Fixed by expanding the ceiling/floor outward to stay strictly beyond the seed's true lightness instead, so every step is computed from the exact same anchor.

Verified with 7 seed cases including the reported one and both extremes (pure white, pure black): every case hits exact step-500 match AND full monotonic ordering (checked numerically via OKLCH lightness comparison, not eyeballed). `tsc` clean, real `npm run build` passes. Regenerated `master-reference`'s primary/secondary scales (light + dark, 48 rows) with the corrected algorithm and confirmed live via the MCP: `"color-primary-500": "#2a6500"` now exactly matches `"primary": "#2a6500"` (bootstrap framework output, unambiguous hex comparison).

**This is now the most validated finding of the whole session** — found by a real third-party AI tool actually consuming the product the way a real customer would, not by us testing in isolation. Confirms the ingestion-testing strategy is working exactly as intended: validate the real-world loop, let it surface what's actually broken, then fix that instead of guessing.

**✅ Retroactively fixed the same scale-anchoring drift in every other real design system, not just the fixture.** The anchor fix above only corrected `master-reference`'s data directly; checking the other 6 real design systems in the DB (`another test`, `Test ABC`, `tester`, `Testing 123`, `theone`, `yui`) found every single one still had a `color.primary.500`/`color.secondary.500` that didn't match its own base color — they'd all been generated under the pre-fix algorithm and the code fix only prevents *future* drift, it doesn't retroactively correct existing data. Regenerated all 240 affected `token_values` rows (primary + secondary, light + dark, scale steps + foreground) with the corrected `materialColorScale`/`materialDarkInvert`/`ctaDarkPair` functions and applied via direct SQL. Every real design system in the product now has a correct step-500 anchor, not just the test fixture.

## Sanity-checked product vision (2026-06-23)

User described the target end-to-end flow: paste a complete or partial token set (structured JSON, OR freeform prose like "font: Inter, primary: blue, radius: 8px"), OR fill in a few global settings visually → gap-fill flushes it out into a complete master file → pick an output framework → get an MCP/GET URL that serves a genuinely good-looking design system any AI vibe-coding tool will respect.

Assessed against everything built so far. Three gaps identified, prioritized as follows — **#2 done this session, #1 and #3 recorded as future steps:**

1. **✅ Built and verified end-to-end 2026-06-24: Freeform/unstructured text → tokens.** Realized as a narrower, more buildable scope than originally framed — not a 10th parser format mapping to the full 26-category schema, but a one-shot AI extraction of the same minimal field set Quick Start already collects (primary/secondary color, font, border radius) from prose, reusing the exact same trusted conversion path `quickStartImport` uses (`cssColorToValue`, `fontFallback`, etc.) rather than inventing new validation. `src/lib/ai/freeform-ingest.ts` (`extractFreeformTokens`, Haiku-tier), new `freeformImport` server action (`src/app/dashboard/[id]/actions.ts`) mirroring `quickStartImport`, new "Describe it" tab in `ImportPanel`/`FreeformImportForm`.
   - **✅ Decided: one-shot background extraction only, no chat interface.** The AI runs once per paste to parse/map the text; the user never converses with it. This keeps the cost ceiling at exactly one call per paste, with no multi-turn cost multiplication.
   - **✅ Decided: input capped at ~4,000 characters** (`FREEFORM_INPUT_CHAR_CAP`). Longer pastes are rejected with a clear message rather than silently chunked — keeps worst-case cost per call predictable and bounds the abuse vector specific to this feature.
   - **✅ Decided: cheap/fast model tier (`claude-haiku-4-5`), not the stronger tier used for color-fill.** Extraction is closer to classification than aesthetic judgment.
   - **✅ Decided: ambiguous items surface to the user, no automatic AI re-prompt.** Anything mentioned that isn't one of the four extractable fields (e.g. "tone of voice should be playful") is returned verbatim in an `ambiguous` array and shown to the user, never guessed at or silently dropped.
   - **✅ Decided: 25 AI imports/month cap**, same pattern as color-fill's `ai_usage`-backed cap (`FREEFORM_INGEST_MONTHLY_CAP`) — reused the same figure since freeform ingestion's per-call cost is already lower than color-fill's, so the same cap leaves more headroom.
   - **Verified live, real browser click → real Haiku call → real DB write:** description *"primary brand color is a deep forest green, around #1a5c2e... secondary should be a warm coral, roughly #e8704a... font Poppins... rounded corners, about 14px... tone of voice should be playful and casual"* → correctly extracted `#1a5c2e`/`#e8704a`/`Poppins`/`14px`, correctly fed through Quick Start's color/font validation and gap-fill, correctly flagged "tone of voice: playful and casual" as ambiguous rather than inventing a token for it. Real cost: $0.0009/call (Haiku, 654 in / 56 out tokens).
2. **✅ Done: gap-fill now produces all 26 categories.** `computeGapFill` (`src/lib/gap-fill/index.ts`) extended with sections 9–15 covering every previously-missing category: font-weight, line-height, letter-spacing, paragraph-spacing, text-decoration, text-transform, shadow, drop-shadow, text-shadow, border-width, border-style, border, opacity, z-index, duration, easing, transition, animation, sizing, component — plus the 21 shadcn-required color roles (card, popover, accent, input, ring, chart.1-5, sidebar.* ) added in section 4c, derived from already-resolved roles (background/muted/border/primary/secondary/semantic), never inventing a new hue. New defaults live in `src/lib/gap-fill/scales.ts` (`DEFAULT_FONT_WEIGHT`, `DEFAULT_SHADOW`, etc.) — same fixed-default convention already used for spacing/radius/breakpoints, and the same values used to hand-build the `master-reference` fixture, so a real user's gap-filled design system and the fixture are now structurally identical in coverage. No changes needed to the persistence layer (`runGapFill` in `src/app/dashboard/[id]/actions.ts`) — its token/value insert and dark-mode-color-only filtering were already category-agnostic. Verified via a manual e2e script (`e2e-gapfill-test.mts`, committed) simulating a Quick-Start-style input (primary, secondary, radius, font only) → confirmed 134 derived tokens across all 26 categories, with the user-provided font correctly preserved rather than overwritten. Confirmed `npx tsc --noEmit` and a real `npm run build` both pass clean.
3. **(Future) No aesthetic-quality mechanism.** "Beautiful, aesthetically pleasing" isn't guaranteed by anything that exists — this is the literal reason the session pivoted away from polishing `src/lib/gap-fill/material.ts`'s OKLCH math after several unsatisfying rounds. The plan's answer (step 3, "AI populates missing tokens") — AI proposes values, deterministic a11y checker validates them — hasn't been built. Same scope as step 3 of "The plan" above.

## Visual preview per target framework — scoped 2026-06-24, not built yet

Originally a loose feature idea; now scoped. Want to see what the design system actually looks like once shaped for a chosen target framework, not just inspect raw/presented JSON — extends the dashboard's existing "Live preview" panel (already shows a generic mockup with a Light/Dark toggle) rather than a new tab.

- **✅ Decided: real framework-authentic rendering, not a generic value-only preview.** Considered a cheaper alternative first — reusing the existing generic `DesignSystemPreview` component fed with each framework's presented values, which would expose value/coverage differences (unmapped tokens) but not real component-level styling differences. Rejected: explicitly wanted true fidelity (e.g. Bootstrap's actual `.btn`/`.card` look, not a Tailwind-shaped mockup wearing Bootstrap's colors).
- **✅ Decided: framework selector lives in the existing Live Preview panel**, next to the current Light/Dark toggle, defaulting to the design system's configured `target_framework`.
- **Real per-framework difficulty is uneven — this is the key implementation fact driving sequencing:**
  - **Tailwind v4 / shadcn / css-variables — cheap.** All three are CSS custom properties under the hood (`@theme {...}` / `:root {...}`). A vendored, fixed set of real components (same pattern already proven this session in the `.scratch-tw-test`/`.scratch-tw-pure-test` scratch projects) just needs fresh CSS variable *values* injected at runtime on every token edit — no compiler, no server round-trip, near-instant.
  - **Tailwind v3 / Bootstrap — expensive.** Tailwind v3 bakes its theme into a JS config at build time, not CSS variables. Bootstrap is the hardest case: its SCSS bakes static values via Sass functions (`shade-color()`, etc.) at compile time — no CSS-variable escape hatch like v4/shadcn have. Real-time preview for these needs an actual `sass`-package compile on a server endpoint, on every change, cached/debounced so it's not recompiling per keystroke. New infrastructure, not yet designed.
- **✅ Decided: sequence as two passes, not one.** Pass 1 ships Tailwind v4 + shadcn + css-variables (no new backend infra, near-instant updates) using the vendored-component approach. Pass 2 adds Bootstrap + Tailwind v3 once the compile-on-demand endpoint (caching/debounce strategy) is designed — explicitly not bundled into the first ship, so the easier 3 frameworks aren't blocked on the hardest, least-proven piece.
- **✅ Pass 1 (shadcn only) built and verified end-to-end 2026-06-24.** Scoped down further mid-build: rather than all three Pass-1 frameworks at once, did shadcn alone first since this app already runs real shadcn/ui components (`src/components/ui/`) — fastest path to genuine fidelity, not a vendored copy. `fetchPreviewTheme` (`src/lib/mcp-client.ts`) gained an optional `framework` param; `page.tsx` now fetches both `css-variables` (existing) and `shadcn`-shaped theme in parallel. New `ShadcnFrameworkPreview` (`src/components/shadcn-framework-preview.tsx`) renders real `Button`/`Card`/`Input`/`Label` inside a wrapper div whose inline style sets `--primary`, `--card`, etc. (the design system's `framework=shadcn` MCP output, prefixed with `--`) — these cascade correctly to any real component nested inside, since this app's own `globals.css` already defines `--color-primary: var(--primary)` etc. (`@theme inline`), so overriding the inner `--primary` on a scoped wrapper needs no build step or recompile. `DesignSystemPreviewPanel` gained a "Generic"/"shadcn" toggle next to the mode switcher.
  - **Verified live:** real button click → real shadcn `Button` (all 6 variants, including shadcn's actual soft-tint `destructive` style, not a solid fill) → `getComputedStyle` confirms `oklch(0.449 0.136 137.1)`, an exact match for `master-reference`'s real `color.primary` value confirmed earlier this session. Dark-mode toggle re-verified via direct computed-style inspection (`background-color: oklch(0.182 0 0)` on the actual rendered Card, correctly dark) after the screenshot tool returned a stale/cached frame that contradicted it — computed style is the authoritative check, per the verification skill's own guidance not to trust screenshots for color values.
  - **Tailwind v4 / css-variables (the rest of Pass 1) — not yet built.** shadcn proved the CSS-variable-override mechanism; Tailwind v4 plain utilities should work the same way but hasn't been wired up. Bootstrap/Tailwind v3 (Pass 2, real Sass compile) remain undesigned, as originally scoped.

## Product UI/chrome polish — added 2026-06-24, not scoped yet

ToknHost's own app shell/UI (dashboard chrome, nav, cards, forms — not the *design-system preview*, which is a separate already-built feature) should look more polished/refined before launch. "Sexier" is a direction, not a spec — needs a concrete scoping pass (which screens, what specifically feels unpolished, any reference inspiration) before it's buildable. Captured here so it isn't lost; not started.

## V2 — explicitly deferred past launch

Anything here either gets resolved before V1 ships, or stays on this list and ships later. Items only land here once that decision has actually been made — don't add something "to be safe."

- **Figma export.** Removed from all selectable framework pickers (`McpAccessCard`'s `FRAMEWORKS` array, and the `framework` enum on the MCP `get_tokens`/`get_theme` tool schemas in `supabase/functions/design-system-mcp/index.ts`) on 2026-06-24, deployed as v9. Reason: only ever API-shape-verified, never proven through the real Figma plugin/desktop app — that needs the user's own Figma account, which isn't available in this environment. The underlying presenter (`presentFigmaVariables` in `lib/present.ts`) and import-side parser (`src/lib/parsers/figma-variables.ts`) are untouched and still fully functional — the plain GET route doesn't enum-validate `framework`, so `?framework=figma-variables` still works if explicitly requested. Promote back to V1 once verified against a real Figma account.

**Moved back to V1 scope on 2026-06-24** (previously listed here):
- ✅ Freeform/unstructured text → tokens — built and verified end-to-end, see above.
- ✅ Aesthetic-quality mechanism for gap-fill (AI color-fill) — built and verified end-to-end, see above.
- Visual preview per target framework in the dashboard — scoped, not built yet (next up).

**Added 2026-06-24:**
- **UX principles** (date formatting, tone of voice, and the rest of the "10 principles" list) — entirely greenfield, nothing built. Blocks "The plan" step 5 ("Build UI to toggle UX tokens"), which is also pushed to V2 as a result.

## Project setup

Git initialized and pushed to `https://github.com/swellben/toknhost.git`, `main` branch, initial commit `76cdb81`. `.env*` confirmed excluded via `.gitignore` before the first push. Going forward: commit as work happens (per user instruction), but only push when explicitly asked — pushes are treated as a confirm-first action.

## Reference data — quick lookup

- Supabase project id: `gxpqjpwsswhgdufyyxlt`
- Master reference design system id: `f994089e-fff3-4870-839f-233af1757628`
- Master reference slug: `master-reference` (public, no MCP token needed)
- Light mode id: `f63903b4-5cd3-4bb0-a868-6f8cbfb6adb6`
- Dark mode id: `b41d4212-7e65-42ce-adbd-7d3075638f79`

## Open questions

- **✅ Decided 2026-06-24: master semantic list is fixed for V1, not user-extensible.** Users cannot add a token path beyond the existing 26-category list — `computeGapFill` keeps operating against that one closed schema. Per-category extensibility (letting a user add a brand-new token path) is V2.
  - Separately confirmed: per-consumer field completeness (does Bootstrap/shadcn/Tailwind get exactly the fields it needs) is a different, already-solved problem — see `BOOTSTRAP_PATH_TO_VAR`/`SHADCN_PATH_TO_VAR`/`namespaceForV4` in `supabase/functions/design-system-mcp/lib/present.ts`, each verified against a real compiler this session (0 unmapped for Bootstrap/Tailwind v4/v3; shadcn's 173 unmapped is correct by design since shadcn's own theme has no concept of color scales/shadows/typography sizing).
- **✅ Decided 2026-06-24: AI-fill lives in a Next.js server action**, alongside `runGapFill` in `src/app/dashboard/[id]/actions.ts` — natural fit since "AI proposes, deterministic checker validates" extends code already there, and it's the standard place to call the Vercel AI SDK from. Not built yet.
  - **New constraint from this decision:** must support per-call cost metering/limits from the start, not bolted on later — user is concerned about misuse/overuse (e.g. repeated re-runs generating large token volumes) eating into margin.
  - **✅ Decided 2026-06-24: AI-fill scope is color generation only**, not a full 26-category replacement. AI proposes the color scale/palette values (the actual aesthetic-quality pain point); everything else stays on `scales.ts`'s existing deterministic defaults. Smaller, cheaper calls, and keeps the existing a11y contrast checker (`src/lib/a11y/`) as the validation safety net — it only covers color anyway, so a full 26-category AI replacement would have left every non-color category with no deterministic check at all.
  - **✅ Decided 2026-06-24: use the stronger/reasoning model tier, not the cheap/fast tier** — explicit aesthetic-quality priority. Product is aimed at product designers with a trained eye for color; poor output directly costs trust, which outweighs the modest per-call cost difference.
  - **Rough cost estimate (pre-build, to revise with real `usage` data once shipped):** ~500-800 input tokens (rules + schema + base color(s)) + ~800-1,500 output tokens (≈20-25 derived color tokens as JSON) per call. ≈$0.015-0.025/call on the chosen stronger tier (~$3/M in, $15/M out). Per-user: ~$0.10-0.25 for 5-10 iterations/session; ~$0.30-0.50/month even at 20 runs/month.
  - **Metering requirement:** log real per-call `usage` (input/output token counts) from the AI SDK from day one rather than relying on estimates; enforce a per-user free-tier cap server-side in the same server action (not just UI-level, since the action could otherwise be hammered directly).
  - **✅ Decided 2026-06-24: free-tier cap is 25 AI color-fill runs/month, presented to the user as a plain run count, not a dollar figure.** First landed on 50 (≈$1 target ÷ ~$0.02/call midpoint), then explicitly cut to 25 — "way more than enough," and a tighter cap creates a clearer upsell moment for anyone who'd actually hit it (signals genuine heavy use, not casual iteration). Actual monthly cost at 25 runs lands well under the original $1 target.
  - **✅ Decided 2026-06-24: no new "AI-fill colors" button — the existing "Run gap-fill" button keeps its name, but now invokes AI for colors instead of the deterministic `materialColorScale` math.** AI fully replaces the deterministic color step (not a parallel/separate option); everything else gap-fill does (the other 25 categories) stays deterministic. User-facing label and mental model stay exactly as they are today.
  - **✅ Decided 2026-06-24: failed accessibility checks are flagged only, never auto-fixed — not even by AI.** Matches today's behavior exactly (`src/lib/a11y/` reports, never corrects). No retry-with-feedback loop. Keeps the cost ceiling at exactly one AI call per gap-fill run and avoids a whole new "AI fixes a11y" scope that was never asked for.
  - **✅ Prompt tweaked 2026-06-25 with modern-palette guidance, verified with real calls.** User found 2026 color-design advice (named palette archetypes, "tinted neutral" principle, 60-30-10 rule, glassmorphism). Assessed each piece against what this tool actually does (extend a user-chosen seed into a 50-950 ramp — it doesn't invent the seed itself or compose page layout): the 60-30-10 rule, glassmorphism/blob gradients, and the four named full palettes don't apply (composition/page-effects guidance, or assume the tool picks colors from scratch, which it doesn't) — flagged rather than silently adopted. The "tinted neutral" principle and avoiding dated "startup blue" flatness *do* apply and were added to the system prompt: explicit instruction that the lightest/darkest steps should stay tinted with the seed's hue rather than drifting toward neutral gray, and to favor contemporary saturated/tactile tones over flat 2010s defaults. Verified with two real calls: an uncommon seed (`#6e4a3f`) produced a scale with hue held essentially constant end-to-end (39.46° → 39.39°) and chroma that stayed nonzero and rose toward the dark end (0.007 → 0.017) rather than collapsing to gray — the guidance is doing real work. **Known limitation found, not fixed:** a famous seed (`#3b82f6`, Tailwind's exact `blue-500`) made the model reproduce Tailwind's own default scale verbatim rather than reasoning fresh — still valid/well-formed output, just a sign the model can shortcut to a memorized palette for very well-known hex values instead of applying the stated principles.
  - **Tried and reverted 2026-06-25: pushing `success`/`warning`/`danger` (deterministic, NOT AI-generated — `SEMANTIC_DEFAULTS` in `gap-fill/index.ts`) more vivid.** User correctly pointed out gap-fill *does* invent colors beyond pure scale-extension when only a primary is given: `background`/`muted`/`border` are semi-invented (derived from primary's hue at a fixed neutral recipe via `materialSurfaces`), and `success`/`warning`/`danger` are *fully* invented (fixed hue/lightness/chroma, no relation to the user's seed at all) — confirmed by reading the code, not assumed. Decided to leave the hues alone (red=danger/green=success convention carries real meaning) but try making them feel less flat by increasing chroma. First attempt (same lightness, higher requested chroma) was a verified no-op — the original chroma values were already at each hue's true sRGB-gamut ceiling at that lightness (`clampChroma` was silently capping them; round-trip-tested to confirm identical hex output). Second attempt searched a lightness band near the original for each hue's real peak-chroma point, found a genuinely higher ceiling (success 0.175→0.205, warning 0.158→0.169, danger 0.220→0.255) — but real-world contrast testing against `color.background` showed this regressed success/danger from PASSING AA as plain text (4.81/5.11) to FAILING (3.30/3.74), a real usage pattern the accessibility checker tests for. Reverted to the original values; the badge/button-fill usage (color + its own `.foreground`) was unaffected either way, but plain-text usage wasn't worth trading away for the vividness gain.
  - **✅ Decided 2026-06-24: a new "Update design system" flow replaces ad-hoc per-token AI regeneration.** Editing an individual color token (e.g. the primary swatch in the Colors tab) saves that field's raw value immediately and cheaply — no AI call. The AI-driven full-palette regeneration only fires when the user explicitly clicks **"Update design system"** (renamed from the Settings panel's current "Save changes"). This keeps the 25/month cap meaningful — a designer trying 10 different primary colors in one session before settling costs zero AI runs; only the deliberate "commit this" click does.
    - **Needs a dirty-state UI signal** (not yet designed) so the user notices they've changed a base color but haven't yet committed/regenerated — e.g. the button label changes (\"Update design system *\" or similar) or an inline alert appears near it. Without this, a user could edit `color.primary`, see the swatch change, but never realize the rest of the palette (scale steps, dark mode, foregrounds) is now stale until they explicitly click Update.
    - **"Update design system" auto-chains the full recheck in one click** — saves the settings fields, runs the AI color regeneration, then automatically re-runs the gap/coverage check and the accessibility check in the same action. The user does not need to separately click "Run gap-fill" or "Run accessibility checks" afterward.
    - **"Run gap-fill" button becomes conditionally visible** — shown only when an actual gap is detected (e.g. right after initial creation before the first fill). Once a design system is fully filled, the button hides; "Update design system" already handles the recheck automatically when a color changes, so there's no remaining case where a user needs to find and click "Run gap-fill" manually in the common flow.
  - **✅ Built and verified end-to-end 2026-06-24.** `src/lib/ai/client.ts` (explicit `baseURL` override — see below), `src/lib/ai/color-fill.ts` (`generateColorFill`, step-500 pinned to the literal seed, monotonicity-checked, falls back to deterministic `materialColorScale` per-seed with zero extra AI cost on failure), `supabase/migrations/005_ai_usage.sql` (applied live). `computeGapFill` (`src/lib/gap-fill/index.ts`) gained `colorOverrides` + exported `findColorScaleSeeds`/`NON_SCALE_COLOR_ROOTS`. `runGapFill` (`src/app/dashboard/[id]/actions.ts`) gained a `forceColorRegen` param: false = today's "fill gaps only" behavior (now AI-backed for colors), true = regenerate every base color's scale from scratch (used by "Update design system"). `updateTokenValue` (`token-actions.ts`) no longer calls any regeneration — edits just save the raw value. `updateDesignSystem` (`src/app/dashboard/actions.ts`) now chains: save settings → `runGapFill(id, true)` → `runAccessibilityChecks(id)`. Button renamed "Update design system" in `edit-design-system-form.tsx`. Verified by real browser clicks against a live dev server: real Anthropic API calls, real DB writes, real `ai_usage` rows, real screenshot of the regenerated palette.
  - **Real bug found and fixed during this build: `ANTHROPIC_BASE_URL` env leak.** The Claude Code/Desktop host process sets `ANTHROPIC_BASE_URL=https://api.anthropic.com` (missing `/v1`) in its own shell environment, which leaks into any child process — including `next dev` and ad-hoc test scripts run via the Bash tool — and silently breaks every Anthropic SDK call with a 404. Fixed by always constructing the provider via `createAnthropic({ apiKey, baseURL: "https://api.anthropic.com/v1" })` in `client.ts` rather than the default `anthropic(modelId)` singleton, which reads the env var.
  - **Real bug found and fixed: `z.record()` (open-ended schema keys) silently breaks Anthropic structured outputs.** A dynamic Zod schema keyed by `z.record(z.string(), ScaleSchema)` made the model return `{}` for every call — confirmed via a controlled test where only the schema shape changed. Fixed by building the schema per-call from the actual known seed names (`z.object({ [name]: ScaleSchema, ... })`) instead of an open-ended record.
  - **Real bug found and fixed: oversized AI calls from an over-inclusive base-color filter — found via the actual "Update design system" flow, not by code review.** The force-regen filter (and, it turned out, the *original* deterministic `computeGapFill` baseColors filter too) treated every flat color token without a "this is a leaf" suffix as "a base color needing a 50–950 scale" — including `background`, `muted`, `border`, `success`, `warning`, `danger`, which should always stay flat single-value tokens. On any *second* gap-fill run (these roles already exist as real tokens by then), this generated spurious `.50`–`.950` children for all of them. At 22 seeds in one AI call this hit Anthropic's structured-output grammar-size limit ("compiled grammar is too large") and threw an **uncaught** 400 that silently aborted the entire `updateDesignSystem` action — no error shown, no DB writes, not even the unrelated accessibility recheck ran. Root-caused via `preview_logs` (server-side stdout, not just the browser console) after extensive guessing failed. Fixed at the source: added `background`/`muted`/`border`/`success`/`warning`/`danger` to `NON_SCALE_COLOR_ROOTS` (`src/lib/gap-fill/index.ts`, now exported) and reused that single denylist in the force-regen filter instead of duplicating an incomplete one. Also wrapped the AI call in `try/catch` so a future API failure degrades to the deterministic fallback instead of crashing the whole action.
  - **Found and cleaned up: this exact bug already existed in real data, predating this session's build.** `Master Reference`, `another test`, and `Testing 123` each had 66 spurious tokens (`color.background.50`–`.950`, etc.) from an earlier double-run of the plain (non-AI) "Run gap-fill" — confirms the *original* deterministic filter had this same flaw all along, just never triggered before because nobody had re-run gap-fill twice on the same design system. Deleted all 198 spurious tokens (with explicit confirmation) after the root-cause fix landed, so no new ones can reappear.
  - **Not yet built:** the dirty-state UI treatment (label vs. alert vs. both) so a user notices an edited-but-not-yet-updated color before clicking "Update design system." "Run gap-fill"'s conditional visibility (hide once nothing is missing) also not yet implemented — it still always shows.
- **✅ Decided 2026-06-24: "10 UX principles" (date formatting, tone of voice, and the rest of the list) is V2, not V1.** Moved to the V2 list below — see "The plan" step 5 above, which depended on this list existing; that step is now blocked on V2 too.
